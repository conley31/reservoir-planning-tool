apps:
  - script   : ./server.js
    name     : 'server'
    watch  : true
    ignore_watch :
      - '.db.log'
    env    :
      NODE_ENV: 'development'
    env_production:
      NODE_ENV: 'production'
